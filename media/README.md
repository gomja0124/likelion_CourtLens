# media

영상 파일처럼 용량이 큰 미디어 자산의 기준 디렉터리입니다.

- `clips/`: 프론트에서 재생하는 잘린 하이라이트 클립
- `source/`: 원본 전체 경기 영상을 둘 위치

`clips/`의 `.mov` 파일은 Git LFS로 관리합니다. `source/`는 현재 데모 실행에는 필요하지 않으며, 원본 영상 기반 재생성 작업을 할 때 사용합니다.
